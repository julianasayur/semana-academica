# Interface

- React com Vite. Rodar: `npm run dev`. Testes: `npm test`, que roda `vitest run`.
- Nos testes, a API é sempre falsa; nunca chame a API real.
- Em desenvolvimento, as chamadas para /salas e /atividades passam pelo proxy do Vite para http://localhost:3000.